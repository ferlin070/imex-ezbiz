{
  "name": "imex-ezbiz-root",
  "version": "1.0.0",
  "description": "Root delegator for imex-ezbiz MARA Agentic Restructure",
  "scripts": {
    "dev": "npm --prefix apps/web run dev",
    "build": "npm --prefix apps/web run build",
    "start": "npm --prefix apps/web run start",
    "test": "node --import tsx --test tests/**/*.test.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tsx": "^4.19.2"
  },
  "workspaces": [
    "apps/web"
  ]
}
